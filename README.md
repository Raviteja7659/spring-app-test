# Spring Boot Simple Web App

A minimal Spring Boot web application with a single REST endpoint.

## Run

```bash
cd "c:\Users\Ravi Teja\Desktop\Spring- App"
mvn spring-boot:run
```

## Endpoint

- `GET /` → returns `Hello from Spring Boot!`
